# 5.Smallest Multiple 
# Find the smallest positive number that is evenly divisible by all of the numbers from 1 to 20?

import math

def func():
	op = 1
	for i in range(1, 21):
		op =op * i // math.gcd(i, ans)
	return op
